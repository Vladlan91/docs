---
title: Messenger Integrations
sidebarTitle: Messengers
description: "Connect Telegram, WhatsApp, Instagram, and Facebook Messenger"
---

## Supported Messengers

| Messenger | Auth Method | Inbound | Outbound | Status |
|---|---|---|---|---|
| **Telegram** | Bot Token | Yes | Yes | Phase 1 |
| **WhatsApp Business** | Meta OAuth | Yes | Yes | Phase 3 |
| **Instagram DM** | Meta OAuth | Yes | Yes | Phase 3 |
| **Facebook Messenger** | Meta OAuth | Yes | Yes | Phase 3 |

## Telegram

Telegram is the simplest integration — no OAuth, no SDK dependencies. Just a bot token from BotFather.

### Setup

<Steps>
  <Step title="Create a Bot">
    Open [@BotFather](https://t.me/BotFather) in Telegram, send `/newbot`, and follow the instructions. You'll receive a **bot token** like `123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11`.
  </Step>
  <Step title="Add Integration">
    Go to **Integrations** → **Add Integration** → select **Telegram** from the Messengers category.
  </Step>
  <Step title="Enter Token">
    Choose a company, name the integration, and paste your bot token.
  </Step>
  <Step title="Verify & Save">
    Revol calls the Telegram Bot API to verify the token and automatically sets up the webhook. Your bot starts receiving messages immediately.
  </Step>
</Steps>

### Agent Tools

| Tool | Description |
|---|---|
| `send_message` | Send a text message to a chat |
| `send_file` | Send a document, photo, or media file |
| `get_chat_history` | Retrieve recent messages from a chat |
| `get_chat_info` | Get chat/user metadata |
| `edit_message` | Edit a previously sent message |
| `delete_message` | Delete a message |

### Webhook Flow

```
User sends message in Telegram
    → Telegram forwards to Revol webhook
    → Revol routes to assigned AI agent
    → Agent processes and generates response
    → Revol calls Telegram Bot API to send reply
    → User sees response in Telegram
```

---

## WhatsApp Business

<Note>
  WhatsApp integration is in development (Phase 3). This section describes the planned implementation.
</Note>

WhatsApp Business API is accessed through Meta's Graph API. Requires a Meta Business account and WhatsApp Business API setup.

### Setup

<Steps>
  <Step title="Meta Business Setup">
    Create a [Meta Business Account](https://business.facebook.com/) and set up WhatsApp Business API access.
  </Step>
  <Step title="Add Integration">
    Go to **Integrations** → **Add Integration** → select **WhatsApp Business**.
  </Step>
  <Step title="OAuth">
    Click "Connect" — you'll be redirected to Meta's consent screen. Authorize access to your WhatsApp Business account.
  </Step>
  <Step title="Verify & Save">
    Revol exchanges the code for a long-lived token (60 days) and configures the webhook.
  </Step>
</Steps>

### Agent Tools

| Tool | Description |
|---|---|
| `send_message` | Send a text message |
| `send_template_message` | Send a pre-approved template message |
| `send_media` | Send an image, document, or video |
| `get_message_history` | Retrieve conversation history |
| `get_contact_info` | Get contact profile information |
| `mark_as_read` | Mark messages as read |

---

## Instagram DM

<Note>
  Instagram integration is in development (Phase 3).
</Note>

Instagram Direct Messages are accessed through Meta's Graph API, same OAuth flow as WhatsApp.

### Agent Tools

| Tool | Description |
|---|---|
| `send_message` | Send a direct message |
| `get_message_history` | Retrieve DM conversation history |
| `get_profile` | Get Instagram user profile |
| `get_comments` | Get comments on a post |
| `reply_to_comment` | Reply to a comment |

---

## Facebook Messenger

<Note>
  Facebook Messenger integration is in development (Phase 3).
</Note>

Facebook Messenger connects via your Facebook Page. Same Meta OAuth flow.

### Agent Tools

| Tool | Description |
|---|---|
| `send_message` | Send a text message |
| `send_template_message` | Send a structured template |
| `send_media` | Send an image, document, or video |
| `get_message_history` | Retrieve conversation history |
| `get_user_profile` | Get user profile from Messenger |

---

## Message Flow (All Messengers)

Regardless of the messenger, the processing flow is identical:

<Steps>
  <Step title="Incoming Message">
    Messenger platform sends a webhook to Revol with the message content and sender info.
  </Step>
  <Step title="Route to Agent">
    Revol identifies the integration, finds the assigned AI agent, and creates or continues a conversation.
  </Step>
  <Step title="AI Processing">
    The message is processed through the agent's workflow — LLM nodes, RAG knowledge base lookup, tool calls, conditions.
  </Step>
  <Step title="Outbound Reply">
    Agent's response is sent back through the same messenger's API. The user sees the reply in their chat.
  </Step>
</Steps>

## Webhook Security

| Messenger | Validation |
|---|---|
| Telegram | Token-based URL (webhook URL contains the bot token hash) |
| WhatsApp | Meta signature verification (X-Hub-Signature-256) |
| Instagram | Meta signature verification (X-Hub-Signature-256) |
| Facebook | Meta signature verification (X-Hub-Signature-256) |
